- [ ] Show webhook for audiows for 46elks in provider view

- [ ] Dont render all entries in lists, render them as the user scrolls. Rendering all makes the app slow

- [ ] We preload call history and conversations, we should preload everything when app starts. especially numbers

- [ ] We should also enrich the data on app start, phone numbers, contacts etc. We need to be smart and only make one contact lookup per phone number.