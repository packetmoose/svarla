# Required
POSTGRES_PASSWORD=a_strong_database_password
INITIAL_PASSWORD=your_login_password
PUBLIC_IP=203.0.113.10

# Optional
BASE_URL=https://your-server.example.com
CORS_ORIGIN=https://your-server.example.com
CONFIG_ENCRYPTION_KEY=

# Caddy TLS (only needed with docker-compose.caddy.yml)
DOMAIN=phone.example.com
# TLS_CERT=/certs/cert.pem
# TLS_KEY=/certs/key.pem
# CERTS_DIR=./certs
